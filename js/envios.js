// ================== Pestaña: Envíos / Despachos ==================
// Lógica de armado de envíos, remesas, remisiones y despacho.

document.addEventListener('tab:activada', (e) => {
  if (e.detail.tab !== 'envios') return;
  // TODO: inicializar/refrescar la pestaña Envíos al entrar
});
