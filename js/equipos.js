// ================== Pestaña: Equipos ==================
// Lógica del catálogo/selección de equipos que entran en un pedido.

document.addEventListener('tab:activada', (e) => {
  if (e.detail.tab !== 'equipos') return;
  // TODO: inicializar/refrescar la pestaña Equipos al entrar
});
