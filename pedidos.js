// ================== Pestaña: Pedidos ==================
// Lógica de listado/gestión de pedidos.

document.addEventListener('tab:activada', (e) => {
  if (e.detail.tab !== 'pedidos') return;
  // TODO: inicializar/refrescar la pestaña Pedidos al entrar
});
